jobs:
  call-reusable:
    uses: codebase-ai/ci_cd_ai_generator/.github/workflows/main.yml@main
    with:
      run_docker_build: true
      push_docker: true
      request_code_review: true
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      