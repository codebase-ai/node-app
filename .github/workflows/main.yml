name: Node.js CI

on:
  push:
    branches: [node]
  pull_request:
    branches: [node]

jobs:
  ci:
    uses: codebase-ai/ci_cd_ai_generator/.github/workflows/ci.yml@node
    with:
      node-version: '20.x'
      run-tests: true
      run-lint: true
      run-build: true
    secrets: inherit